# Tell Grafana to load any dashboard JSON files from the mounted folder.
apiVersion: 1

providers:
  - name: BundesHost
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
